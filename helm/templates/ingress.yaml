apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "golang-example.fullname" . }}
spec:
  tls:
  - hosts:
    - {{ .Values.host}}
    secretName: {{ .Values.ssl.secret}}
  rules:
  - host: {{ .Values.host}}
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: golang-example
            port:
              number: 443